const jsonTypes = [
    'application/json',
    'application/json-patch+json',
    'application/vnd.api+json',
    'application/csp-report',
    'application/reports+json',
];
export function isJsonBody(ctx, options) {
    return options.json && ctx.is(jsonTypes);
}
export function isUrlencodedBody(ctx, options) {
    return options.urlencoded && ctx.is('urlencoded');
}
export function isTextBody(ctx, options) {
    return options.text && ctx.is('text/*');
}
export function isMultipartBody(ctx, options) {
    return options.multipart && ctx.is('multipart');
}
//# sourceMappingURL=body-type-util.js.map